# Force API-only install (ignore requirements.txt / Playwright).
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements-api.txt"]

[start]
cmd = "uvicorn queud_api.server:app --host 0.0.0.0 --port $PORT"