# Fallback if Dockerfile builder is not used.
[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["python -m pip install -r requirements-api.txt"]

[start]
cmd = "uvicorn queud_api.server:app --host 0.0.0.0 --port $PORT"